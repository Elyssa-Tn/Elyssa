import { extendTheme } from "@mui/joy";

const theme = extendTheme({
  colorSchemes: {
    light: {
      palette: {
        primary: {
          50: "#FFFDD0",
        },
      },
    },
  },
});

export default theme;
